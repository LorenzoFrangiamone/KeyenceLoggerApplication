## Portability
For reproducing the precise venv used for developement it is sufficient to type some commands on PS:

Linux:
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

Windows:
.\.venv\Scripts\activate
pip install -r requirements.txt
